language: php
php:
  - 7.2

services:
  - docker
  
before-install:
  - docker build -t acdh/fcrepo tests/docker
  - docker run -d -p 8080:8080 acdh/fcrepo
  - sleep 5
  - cp config.ini.sample tests/config.ini
  
install:
  - composer update

script:
  - php -f tests/fedora-weak-transactions.php
